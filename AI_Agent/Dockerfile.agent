FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

# Copy entire project folder so ai_agent_service.py is included
COPY . /app

EXPOSE 8001

CMD ["uvicorn", "ai_agent_service:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]
